---
import ContentArticle from "./ContentArticle.astro";
import type { Museum } from "../../data/museums";
import { museums } from "../../data/museums";

interface Props {
  items?: Museum[];
}

const { items = museums } = Astro.props;
---

<section
  class="bg-[#F0F0E4] py-[clamp(72px,9vw,144px)] pb-[clamp(80px,10vw,160px)] text-[#1a1a18]"
  aria-labelledby="contents-title"
>
  <div class="mx-auto w-[min(100%,1440px)]">
    <header
      class="mx-[clamp(24px,7vw,112px)] mb-[clamp(56px,6vw,96px)] pt-[clamp(16px,2vw,32px)]"
    >
      <p class="mb-3.5 text-xs font-bold tracking-[0.14em] uppercase">
        Museum Contents
      </p>
      <h2
        id="contents-title"
        class="m-0 text-[clamp(2rem,6vw,5.5rem)] leading-[0.95] font-extrabold"
      >
        行ってみたい美術館
      </h2>
    </header>

    <div class="grid gap-[clamp(72px,9vw,144px)]">
      {
        items.map((item, index) => (
          <ContentArticle item={item} index={index} total={items.length} />
        ))
      }
    </div>
  </div>
</section>
